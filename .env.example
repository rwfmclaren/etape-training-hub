# PostgreSQL Database Configuration
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=etape_training

# Backend Configuration
SECRET_KEY=your-secret-key-change-in-production-use-openssl-rand-hex-32
DATABASE_URL=postgresql://user:password@db:5432/etape_training

# Frontend Configuration (Build-time variable)
# This variable is used during Docker build and gets baked into the JavaScript bundle
# For local development with docker-compose:
VITE_API_BASE_URL=http://localhost:8000
# For Railway/production deployment, set this to your backend URL:
# VITE_API_BASE_URL=https://your-backend-domain.railway.app

# Optional: CORS Configuration
# CORS_ORIGINS=["http://localhost", "http://localhost:80"]
